foreach(target strings strtok_r strsep)
	add_executable(r_${target} ${target}.c)
	set_target_properties(r_${target} PROPERTIES OUTPUT_NAME ${target})
	target_link_libraries(r_${target} replacements tdsutils ${lib_BASE})
endforeach(target)
